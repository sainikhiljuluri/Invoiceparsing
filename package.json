{
  "name": "ai-invoice-processor",
  "version": "1.0.0",
  "description": "AI-powered invoice processing system with advanced RAG analytics",
  "main": "frontend/index.html",
  "scripts": {
    "start": "python3 -m uvicorn api.main:app --host 0.0.0.0 --port 8000",
    "dev": "python3 -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload",
    "build": "echo 'Static frontend - no build required'"
  },
  "keywords": [
    "ai",
    "invoice",
    "processing",
    "analytics",
    "rag",
    "chat",
    "supabase"
  ],
  "author": "Invoice Analytics Team",
  "license": "MIT",
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
